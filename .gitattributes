# Normalize text in the repo, check out platform-appropriate line endings.
* text=auto

# Shell scripts must keep LF or they break under bash/WSL.
*.sh   text eol=lf

# PowerShell is happiest with CRLF on Windows.
*.ps1  text eol=crlf

# Binary
*.png  binary
