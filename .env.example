# Secrets referenced by CLAUDE.md, memory/ and the ags-monitor skill.
# Real values were deliberately NOT exported. Fill these in on the new machine.
# This file is copied to ~/.claude/.env by install.ps1 and is gitignored.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
