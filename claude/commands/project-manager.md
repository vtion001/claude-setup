---
description: "Project Manager Agent"
---

# project-manager Agent

Run the project-manager agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate project-manager $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/project-manager/ exists and has the correct main script.
