---
description: "Resume Optimizer Agent"
---

# resume-optimizer Agent

Run the resume-optimizer agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate resume-optimizer $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/resume-optimizer/ exists and has the correct main script.
