---
description: "Activate the database-developer agent"
---

# database-developer Agent

Run the database-developer agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate database-developer $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/database-developer/ exists and has the correct main script.
