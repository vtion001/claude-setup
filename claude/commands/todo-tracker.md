---
description: "TODO Tracker Agent"
---

# todo-tracker Agent

Run the todo-tracker agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate todo-tracker $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/todo-tracker/ exists and has the correct main script.
