---
description: "DevOps Orchestrator Agent"
---

# dev-orchestrator Agent

Run the dev-orchestrator agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate dev-orchestrator $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/dev-orchestrator/ exists and has the correct main script.
