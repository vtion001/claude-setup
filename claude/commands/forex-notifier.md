---
description: "Activate the forex-notifier agent"
---

# forex-notifier Agent

Run the forex-notifier agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate forex-notifier $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/forex-notifier/ exists and has the correct main script.
