---
description: "Presentation Agent"
---

# presentation-agent Agent

Run the presentation-agent agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate presentation-agent $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/presentation-agent/ exists and has the correct main script.
