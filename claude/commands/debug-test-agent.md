---
description: "Debug & Test Agent"
---

# debug-test-agent Agent

Run the debug-test-agent agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate debug-test-agent $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/debug-test-agent/ exists and has the correct main script.
