---
description: "Quotation Generator Agent"
---

# quotation-generator Agent

Run the quotation-generator agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate quotation-generator $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/quotation-generator/ exists and has the correct main script.
