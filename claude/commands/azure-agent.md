---
description: "Activate the azure-agent agent"
---

# azure-agent Agent

Run the azure-agent agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate azure-agent $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/azure-agent/ exists and has the correct main script.
