---
description: "Debug Agent"
---

# debug-agent Agent

Run the debug-agent agent with the provided arguments.

## Instructions

Run this command:
```bash
python3 /Users/archerterminez/agent activate debug-agent $ARGUMENTS
```

Show the output to the user. If the agent fails, check that ~/agents/debug-agent/ exists and has the correct main script.
