# Secrets referenced by video-kit's stock-footage/photo sourcing scripts.
# Real values were deliberately NOT exported. Copy this to .env alongside it
# and fill in real keys on the machine that runs this skill — .env itself is gitignored.

PEXELS_API_KEY=
PIXABAY_API_KEY=
